type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
  content: String
  author: User @hasOne
}

type User @model @auth(rules: [{allow: public}, {allow: private}]) {
  id: ID!
  email: AWSEmail!
  user_sub_id: String!
  user_pool_id: String!
  username: String
}
 