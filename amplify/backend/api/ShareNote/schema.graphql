type Posts @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  content: String
  author: User! @hasOne
}

type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  username: String!
  age: Int
  email: String!
}
 